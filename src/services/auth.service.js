import {pool} from 'pg';

const registerUser=({name,email,password})=>{
     return "ok";
}
const loginUser=({email,password})=>{
     return "ok";
}
const loginUser=({email,password})=>{
     return "ok";
}